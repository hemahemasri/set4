#include<stdio.h>
void main()
{
int temp,count=0;
char kh[200];
printf("\n enter the nummber:");
gets(kh);
for(temp=0;d[temp]!=0;temp++)
{
if(kh[temp]>=0&&kh[temp]<=9)
{
count++;
}
}
printf("the numeric values are %d",temp,count);
getch();
}
