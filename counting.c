#include<stdio.h.
void main()
{
int cch=0;
  int cwd=1;
char c='a';
  printf("Enter your sentence in lowercase: ");
  while(ch!='\r')
  {
    ch=getche();
    if(ch==' ')
      cwd++;
    else
      cch++;
  }

  printf("\n%dWords = ",cwd);

  printf("%dCharacters = ",cch-1);

  getch();

}
